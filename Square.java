package Streams;


import java.awt.Event;
import java.awt.im.spi.InputMethodDescriptor;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collector;
import java.util.stream.Collectors;
import java.util.stream.Stream;

import javax.swing.plaf.basic.BasicSplitPaneDivider;

public class Square {

	public static void main(String[] args) {

		List<Integer> n = Arrays.asList(1, 2, 3, 4,6, 5, 7, 8,  9, 11, 2);
		System.out.println(n);
		//		List<Integer>numberquare=n.stream().map(s->s*s).collect(Collectors.toList());
		//		System.out.println(numberquare);
		
	//	multiplication 
		
//		List<Integer>numbersquare=n.stream().map(s->s*s).collect(Collectors.toList());
//		System.out.println(numbersquare);

		//		 addition

//		List<Integer>numberaddition=n.stream().map(s->s+s).collect(Collectors.toList());
//		System.out.println(numberaddition);
		
//							subtraction
		
//		List<Integer>numbersubtraction=n.stream().map(s->s-s).collect(Collectors.toList());
//		System.out.println(numbersubtraction);
	
//		division
		
//		List<Integer>numberdivision=n.stream().map(s->s/s).collect(Collectors.toList());
//		System.out.println(numberdivision);
		
//		divide by 1
//		List<Integer>numberdivision1=n.stream().map(s->s/1).collect(Collectors.toList());
//		System.out.println(numberdivision1);
		
//								subtraction 2
		
//		List<Integer>numbersubtraction1=n.stream().map(s->s-2).collect(Collectors.toList());
//		System.out.println(numbersubtraction1);
		
//		 addition

//		List<Integer>numberaddition1=n.stream().map(s->s+2).collect(Collectors.toList());
//		System.out.println(numberaddition1);
		
//		Even numbers
		
//		List<Integer>evennumber=n.stream().filter(s->s%2==0).collect(Collectors.toList());
//		System.out.println(evennumber);
		
//		oddnumbers
//		List<Integer>oddnumber=n.stream().filter(s->s%2!=0).collect(Collectors.toList());
//		System.out.println(oddnumber);
		
		
//		Even numbers
		
//		List<Integer>evennumber1=n.stream().filter(s->s%2==0).collect(Collectors.toList());
//		System.out.println(evennumber1);
//		
//		odd number
//		List<Integer>oddnumber1=n.stream().filter(s->s%2!=0).collect(Collectors.toList());
//		System.out.println(oddnumber1);
		
		
		
		
		
		
	}

}
